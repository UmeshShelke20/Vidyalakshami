import { EducationPrevHistory } from './education-prev-history';

describe('EducationPrevHistory', () => {
  it('should create an instance', () => {
    expect(new EducationPrevHistory()).toBeTruthy();
  });
});
